class SkylabArray {
  constructor(valor) {
    this.length = valor;
  }

  push(valor) {
    const arrayTest = [];
    return arrayTest.push(this.valor);
  }

  some(valor) {
    //
  }

  find(valor) {
    //
  }

  filter(valor) {
    //
  }

  map(valor) {
    //
  }

  module.exports = {
    push,
    some,
    find,
    filter,
    map,
  };
}
